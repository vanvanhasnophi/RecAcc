# RecAcc

根目录：
`./eleme` 饿了么数据集的分析结果，源数据需自行在本地处理
`./notebooks` 进行实验的笔记本，包括数据预处理，模型训练，评估等
`./paper` 论文Latex
`./quick_run_data` 用于快速测试推荐器的数据
`./reference` 参考文献
`./utils` 工具脚本，可以被notebook引用
`./web` 交互式网页
`结构化路径^.md` notion文章
`requirements.txt` python依赖列表，直接往里面加就行