# 用于机器学习的基于Shapley的公平数据估值
1 斯坦福大学电气工程系，美国加利福尼亚州斯坦福 2 斯坦福大学生物医学数据科学系，美国加利福尼亚州斯坦福。 通讯作者：James Zou <jamesz@stanford.edu>。

Amirata Ghorbani 1 James Zou 2

## 摘要

随着数据成为推动技术和经济增长的燃料，一个根本性的挑战是如何量化数据在算法预测和决策中的价值。例如，在医疗保健和消费者市场中，有人建议个人应因其生成的数据获得补偿，但目前尚不清楚什么是个人数据的公平估值。在这项工作中，我们为监督机器学习背景下的数据估值开发了一个原则性框架。给定一个在 \(n\) 个数据点上训练以产生预测器的学习算法，我们提出数据Shapley作为一种度量，用于量化每个训练数据对预测器性能的价值。数据Shapley独特地满足了公平数据估值的几个自然特性。我们开发了蒙特卡洛和基于梯度的方法，以在复杂学习算法（包括神经网络）在大数据集上训练的实际设置中高效估计数据Shapley值。除了公平性之外，在生物医学、图像和合成数据上的广泛实验证明，数据Shapley还有几个其他好处：1）在提供关于哪些数据对给定学习任务更有价值的见解方面，它比流行的留一法或杠杆分数更强大；2）低Shapley值数据能有效捕捉异常值和损坏；3）高Shapley值数据告知需要获取何种新数据来改进预测器。

数据已成为市场的重要组成部分，类似于劳动力和资本（Posner & Weyl, 2018）。有人建议某些数据构成个人财产，因此个人应因这些数据获得补偿（Regulation, 2018）。与劳动力和资本一样，一个根本性问题是如何公平地评估个人数据的价值。

我们专注于监督机器学习特定背景下的数据估值。为了理解数据的价值，我们的调查需要三个要素：固定的训练数据集、学习算法和性能度量。训练数据是固定的 \(n\) 个数据点的集合，\(\{x_{i},y_{i}\}_{1}^{n}\)，其中 \(x_{i}\) 和 \(y_{i}\) 分别是点 \(i\) 的特征和标签。为了我们的目的，学习算法 \(\mathcal{A}\) 是一个过程，它接受任意训练集并产生一个预测器。例如，\(\mathcal{A}\) 可以是常见的经验风险最小化，其中它求解 \(\theta^{*} = \arg \min_{\theta}\sum l(f(x_{i};\theta),y_{i})\)，其中 \(l\) 是损失，\(\theta\) 参数化模型族，而 \(f(\cdot ;\theta^{*})\) 是预测器。对于任何预测器 \(f\)，我们还需要一个性能度量 \(V(f)\)。我们可以将 \(V\) 视为 \(f\) 在感兴趣的任何度量上的测试性能。

我们想要研究的两个问题是：1）对于学习算法 \(\mathcal{A}\) 和性能度量 \(V\)，每个 \((x_{i},y_{i})\) 的公平价值度量是什么；以及 2）如何在实践设置中高效计算这个数据价值。例如，假设我们有来自 \(N = 1000\) 名患者的数据，我们训练一个小型神经网络来构建心脏病分类器。我们还有一些独立的度量来评估训练后分类器的性能——例如，它在测试集上的预测准确率。那么我们希望量化每个患者的数据对该任务上分类器性能的价值。

## 1. 引言

数据是有价值的，它是驱动人工智能的燃料。在医疗保健和广告等领域，个人生成的数据日益成为关键组成部分

注意，我们没有定义数据的通用价值。相反，每个数据的价值取决于学习算法、性能度量以及训练集中的其他数据。这种依赖性在机器学习中是合理且期望的。如果我们训练的是逻辑回归而不是神经网络，某些数据点可能更重要。类似地，如果性能度量发生变化——例如，回归到心脏病发病年龄而不是心脏病发病率——那么某些患者数据的价值应该改变。此外，性能度量可以在与训练数据不同的人群/分布上计算；我们对其如何计算不做任何假设。
 一种评估数据重要性的常见方法是某种形式的留一法（LOO）测试：比较在完整数据集上训练的预测器性能与在完整集减去一个点的情况下训练的性能之间的差异（Cook, 1977）。性能下降是那个点"价值"的一种度量。LOO通常通过杠杆或影响分数来近似，它衡量当一个点的权重轻微变化时预测器的变化程度（Cook & Weisberg, 1982）。我们将在下面展示，留一法不满足我们期望的公平数据估值的自然特性，并且在实验中表现不佳。举个简单的例子说明为什么留一法失败，假设我们的预测器是一个最近邻分类器——即，对于每个测试点，我们在训练集中找到其最近邻并分配该标签。此外假设每个训练点在训练集中有两个完全相同的副本。从训练中移除一个点完全不会改变预测器，因为它的副本仍然存在。因此，留一法会给每个训练点分配零价值，无论实际预测器性能如何。这个简单的例子说明留一法不能捕捉数据子集之间潜在复杂的交互作用。我们提出的数据Shapley值通过精确地解释这种交互作用提供了更有意义的估值。

**我们的贡献** 我们为机器学习中公平数据估值这一重要问题提供了一个自然的表述。我们提出数据Shapley值，利用博弈论中的强大结果，来量化单个数据点对学习任务的贡献。数据Shapley值独特地满足了公平估值的三个自然特性。此外，我们的实证研究表明，数据Shapley值有几个额外的效用：1）它比常见的留一法分数更深入地揭示了每个数据点的重要性；2）它可以识别异常值和损坏的数据；3）它可以告知如何获取未来的数据来改进预测器。

## 2. 机器学习的公平数据估值

**预备知识** 让 \(D = \{(x_{i},y_{i})\}_{1}^{n}\) 是我们固定的训练集。我们对 \(D\) 不做任何分布假设，数据不需要是独立的。对于分类和回归，\(y_{i}\) 可以是分类的或实值的。让 \(\mathcal{A}\) 表示学习算法。我们将 \(\mathcal{A}\) 视为一个黑盒，它接受大小在0到 \(\infty\) 之间的训练数据集作为输入，并返回一个预测器。我们对在子集 \(S\subseteq D\) 上训练的预测器特别感兴趣。性能分数 \(V\) 是另一个黑盒预言机，它接受任何预测器作为输入并返回一个分数。我们写 \(V(S,\mathcal{A})\)，或简写为 \(V(S)\)，表示在数据 \(S\) 上训练的预测器的性能分数。我们的目标是计算一个数据价值 \(\phi_{i}(D,\mathcal{A},V)\in \mathbb{R}\)，作为 \(D,\mathcal{A}\) 和 \(V\) 的函数，以量化第 \(i\) 个数据的价值。为了简化符号，我们常将其写为 \(\phi_{i}(V)\) 或仅 \(\phi_{i}\)。为了方便起见，我们有时会重载 \(S\) 和 \(D\) 的符号，使其也可以表示索引集合——即，如果 \((x_{i},y_{i})\) 在该子集中，则 \(i\in S\)，并且 \(D = \{1,\dots,n\}\)。

**示例** 假设 \(y_{i}\) 是二元的，并且 \(\mathcal{A}\) 对应于一个逻辑回归学习器——即，\(\mathcal{A}\) 接受任何数据集并返回拟合到它的逻辑回归。这里的分数 \(V\) 可以是在单独测试集上的0/1准确率。那么 \(V(S)\) 是当逻辑回归在子集 \(S\subseteq D\) 上训练时的0/1测试准确率。如果 \(S = \emptyset\)，那么 \(V(S)\) 是随机初始化分类器的性能。通常，用于计算 \(V\) 的测试数据可能与 \(D\) 的分布不同。

**数据估值的公平特性** 我们相信，为了公平，\(\phi\) 应满足以下特性：

1. 如果 \((x_{i},y_{i})\) 添加到任何训练数据子集都不会改变性能，则应给予零价值。更精确地说，假设对所有 \(S\subseteq D - \{i\}\)，有 \(V(S) = V(S\cup \{i\})\)，则 \(\phi_{i} = 0\)。
2. 如果对于数据 \(i\) 和 \(j\) 以及任何子集 \(S\subseteq D - \{i,j\}\)，我们有 \(V(S\cup \{i\}) = V(S\cup \{j\})\)，则 \(\phi_{i} = \phi_{j}\)。换句话说，如果 \(i\) 和 \(j\)，当添加到我们训练数据的任何子集时，总是产生预测器分数的完全相同的变化，那么根据对称性，\(i\) 和 \(j\) 应被赋予相同的价值。
3. 在大多数ML设置中，\(V = -\sum_{k\in \text{test set}}l_{k}\)，其中 \(l_{k}\) 是预测器在第 \(k\) 个测试点上的损失（我们取了负号，以便更低的损失对应于更高的分数）。我们可以定义 \(V_{k} = -l_{k}\) 为预测器在第 \(k\) 个测试点上的性能。类似地，\(\phi_{i}(V_{k})\) 量化了第 \(i\) 个训练点对第 \(k\) 个测试点的价值。如果数据 \(i\) 分别对测试点1和2的预测贡献价值 \(\phi_{i}(V_{1})\) 和 \(\phi_{i}(V_{2})\)，那么当 \(V = V_{1} + V_{2}\) 时，我们期望 \(i\) 在预测两个测试点时的价值是 \(\phi_{i}(V_{1}) + \phi_{i}(V_{2})\)。换句话说：当总体预测分数是 \(K\) 个单独预测的总和时，数据的价值应该是其每个预测价值的总和。形式化地：对于性能分数 \(V\) 和 \(W\)，有 \(\phi_{i}(V + W) = \phi_{i}(V) + \phi_{i}(W)\)。

虽然还有其他值得讨论的数据估值期望特性，但上述列出的这三个特性实际上确定了 \(\phi_{i}\) 的形式，仅差一个比例常数。
 **命题2.1.** 任何满足上述特性1-3的数据估值 \(\phi (D, \mathcal{A}, V)\) 必须具有以下形式

\[\phi_{i} = C\sum_{S\subseteq D - \{i\}}\frac{V(S\cup\{i\}) - V(S)}{(\frac{n - 1}{|S|})} \quad (1)\]

其中和是遍历所有不包含 \(i\) 的 \(D\) 子集，\(C\) 是任意常数。我们称 \(\phi_{i}\) 为点 \(i\) 的数据Shapley值。

**证明.** 方程1中的 \(\phi_{i}\) 表达式与博弈论中定义的Shapley值相同，仅差常数 \(C\)（Shapley, 1953; Shapley等人，1988）。这激励我们称 \(\phi_{i}\) 为数据Shapley值。证明也直接源自博弈论Shapley值的唯一性，通过将我们的问题简化为合作博弈（Dubey, 1975）。在合作博弈论中，有 \(n\) 个玩家，并且有一个分数函数 \(v: 2^{|n|} \to \mathbb{R}\)。基本上 \(v(S)\) 是如果子集 \(S\) 中的玩家一起工作所得的奖励。Shapley提出了一种在 \(n\) 个玩家之间分配分数的方法，以便每个玩家获得他/她的公平支付，其中公平性由在数学上等价于我们列出的三个特性的属性来编码。我们可以将数据估值视为合作博弈：每个训练数据是一个玩家，训练数据通过学习器 \(\mathcal{A}\) 一起工作以实现预测分数 \(v = V\)。数据Shapley值类似于每个玩家获得的支付。

\(C\) 的选择是任意缩放，不影响我们的任何实验和分析。

**数据Shapley的解释** 方程1可以解释为 \(i\) 的所有可能"边际贡献"的加权和；其中权重是 \(D - \{i\}\) 中大小为 \(|S|\) 的子集数量的倒数。这种表述接近于留一法，在留一法中我们考虑最后一个边际贡献 \(V(D) - V(D - \{i\})\)，而我们考虑每个点的边际贡献，假设给出的不是整个训练集，而是它的随机子集。换句话说，我们可以假设这样一个场景：我们获得的不是训练数据，而是它的一个随机子集；Shapley公式通过捕捉所有这些可能的子集场景输出一个公平的价值。

## 3. 近似数据Shapley

如前一节所讨论，方程1中的Shapley公式唯一地提供了对数据点的公平价值分配。然而，计算数据Shapley需要计算所有可能的边际贡献，这在训练数据大小上是指数级的。此外，对于每个 \(S \subseteq D\)，计算 \(V(S)\) 涉及使用学习算法 \(\mathcal{A}\) 在 \(S\) 上学习一个预测器。因此，

计算精确的Shapley值对于现实世界的数据集是不可行的。在本节中，我们讨论近似方法来估计数据Shapley值。

### 3.1. 近似Shapley值

如前所述，计算Shapley值在数据点数量 \(n\) 上具有指数复杂度。在这里，我们讨论两种规避此问题的方法：

**蒙特卡洛方法**： 我们可以将方程1重写为等价形式，设 \(C = 1 / n!\)。让 \(\Pi\) 是所有 \(n!\) 个数据点排列上的均匀分布，我们有：

\[\phi_{i} = \mathbb{E}_{\pi \sim \Pi}[V(S_{\pi}^{i}\cup \{i\}) - V(S_{\pi}^{i})] \quad (2)\]

其中 \(S_{\pi}^{i}\) 是排列 \(\pi\) 中数据点 \(i\) 之前的数据点集合（如果 \(i\) 是第一个元素，则 \(S_{\pi}^{i} = \emptyset\)）。

如方程2所述，计算Shapley值可以表示为一个期望计算问题。因此，已经开发并分析了蒙特卡洛方法来估计Shapley值（Mann & Shapley, 1962; Castro等人，2009b; Maleki等人，2013）。首先，我们采样一个数据点的随机排列。然后，我们从第一个元素扫描到最后一个元素，并计算每个新数据点的边际贡献。在多个蒙特卡洛排列上重复相同的过程，数据Shapley的最终估计就是所有计算出的边际贡献的平均值。这种蒙特卡洛抽样给出了数据Shapley的无偏估计。在实践中，我们生成蒙特卡洛估计，直到平均值经验上收敛。先前的工作已经分析了蒙特卡洛近似Shapley值的误差界（Maleki等人，2013）。附录A中的图6描绘了数据Shapley收敛的例子。在实践中，收敛通常需要数量级为 \(n\) 的样本；通常 \(3n\) 个蒙特卡洛样本足以收敛。

**截断**： 在机器学习设置中，对于 \(S \subseteq N\) 的 \(V(S)\) 通常是在单独测试集上学习的模型的预测性能。因为测试集是有限的，\(V(S)\) 本身是训练模型在测试分布上真实性能的近似，我们不知道这个真实性能。在实践中，估计数据Shapley值达到 \(V(S)\) 中的内在噪声水平就足够了，这可以通过测量同一预测器在测试集的引导样本上的性能变化来量化（Friedman等人，2001）。另一方面，随着 \(S\) 大小的增加，仅添加一个训练点带来的性能变化越来越小（Mahajan等人，2018; Beleites等人，2013）。结合这两个观察结果导出了一个自然的截断方法。
 **算法1 截断蒙特卡洛Shapley**

输入：训练数据 \(D = \{1,\ldots ,n\}\)，学习算法 \(\mathcal{A}\)，性能分数 \(V\) 输出：训练点的Shapley值：\(\phi_{1},\ldots ,\phi_{n}\) 初始化 \(\phi_{i} = 0\)，对于 \(i = 1,\ldots ,n\) 和 \(t = 0\) 当收敛标准未满足时 do \(t\gets t + 1\) \(\pi^t\)：训练数据点的随机排列 \(v_0^t\gets V(\emptyset ,A)\) 对于 \(j\in \{1,\ldots ,n\}\) do 如果 \(|V(D) - v_{j - 1}^{t}|<\) 性能容差 那么 \(v_{j}^{t} = v_{j - 1}^{t}\) 否则 \(v_{j}^{t}\gets V(\{\pi^{t}[1],\ldots ,\pi^{t}[j]\} ,A)\) 结束如果 \(\phi_{\pi^{t}[j]} - \frac{t - 1}{t}\phi_{\pi^{t - 1}[j]} + \frac{1}{t} (v_{j}^{t} - v_{j - 1}^{t})\) 结束 for 结束 for

我们可以基于 \(V\) 的引导变化定义一个"性能容差"。当我们扫描采样的排列并计算边际贡献时，每当 \(V(S)\) 在 \(V(D)\) 的性能容差范围内时，我们就截断该采样排列中的边际贡献计算，并为此排列中剩余的数据点设置边际贡献为零。附录B显示截断带来了实质性的计算节省，而没有引入显著的估计偏差。在本文的其余部分，我们将截断与蒙特卡洛的结合称为"截断蒙特卡洛Shapley"（TMC-Shapley）；在算法1中有更详细的描述。

### 3.2. 近似性能度量 \(V\)

对于每个 \(S\subseteq D\)，计算 \(V(S)\) 需要 \(\mathcal{A}\) 学习一个新模型。对于小的 \(D\) 和快速的 \(\mathcal{A}\)（例如逻辑回归、LASSO），可以按所述使用TMC-Shapley方法。然而，在数据点数量大或预测模型需要高计算能力（例如深度神经网络）的设置中，应用TMC-Shapley可能相当昂贵。我们提出了两种策略，以进一步降低大数据设置中数据Shapley的计算成本。

**梯度Shapley** 对于广泛的预测模型族，\(\mathcal{A}\) 涉及随机梯度下降的变体，其中随机选择的 \(D\) 批次迭代更新模型参数。在这些设置中，完全训练模型的一个简单近似是考虑仅对训练数据进行一次遍历来训练模型；换句话说，我们训练模型一个"周期"的 \(D\)。这

## 算法2 梯度Shapley

输入：参数化和可微的损失函数 \(\mathcal{L}(\cdot ;\theta)\)，训练数据 \(D = \{1,\ldots ,n\}\)，性能分数函数 \(V(\theta)\) 输出：训练点的Shapley值：\(\phi_{1},\ldots ,\phi_{n}\) 初始化 \(\phi_{i} = 0\)，对于 \(i = 1,\ldots ,n\) 和 \(t = 0\) 当收敛标准未满足时 do

\(t\gets t + 1\) \(\pi^t\)：训练数据点的随机排列 \(\theta_0^t\) 随机参数 \(v_0^t\gets V(\theta_0^t)\) 对于 \(j\in \{1,\ldots ,n\}\) do \(\theta_j^t\leftarrow \theta_{j - 1}^t -\alpha \nabla_\theta \mathcal{L}(\pi^t [j];\theta_{j - 1})\) \(v_j^t\gets V(\theta_j^t)\) \(\phi_{\pi^t [j]} - \frac{t - 1}{t}\phi_{\pi^{t - 1}[j]} + \frac{1}{t} (v_j^t -v_{j - 1}^t)\) 结束 for 结束 for

近似很好地适应了算法1的框架：对于采样的数据点排列，通过每次对一个数据点执行梯度下降来更新模型；边际贡献是模型性能的变化。细节在算法2中描述，我们简称其为梯度Shapley或G-Shapley。为了获得最佳近似，我们对学习算法进行超参数搜索，找到仅对数据进行一次遍历训练的模型产生最佳性能的那一个，在我们的实验中，这导致学习率大于用于多周期模型训练的学习率。附录D讨论了G-Shapley方法在本文实验结果中产生的近似有多好的数值示例。

**数据点组的价值** 在许多设置中，为了获得更稳健的解释或因为训练集非常大，我们更愿意计算数据点组的数据Shapley，而不是单个数据点。例如，在心脏病预测设置中，我们可以根据年龄、性别、种族和其他特征将患者分组到离散的箱中，然后量化每个箱的数据Shapley。在这些设置中，我们可以使用与算法1相同的过程计算组的Shapley值，将数据点 \(i\) 替换为组 \(i\)。因此，即使对于非常大的数据集，如果组的数量合理，我们也可以计算组Shapley值。

## 4. 实验与应用

在本节中，我们通过在真实和合成数据上的系统实验展示数据Shapley的估计和应用。我们表明，具有高Shapley值的点对模型的性能至关重要，反之
 亦然。然后我们讨论了获取类似于高价值训练点的新数据点与随机获取新数据相比的效果。此外，我们进行了两个实验，表明有噪声或标签损坏的数据点将被分配低Shapley值。最后，我们展示了Shapley值也可以为个体组提供信息丰富的分数。总之，这些实验表明，除了其公平特性外，数据Shapley还提供了有意义的价值来量化数据的重要性，并可以为下游分析提供信息。鉴于杠杆和影响分数试图近似留一法分数（Koh & Liang, 2017），在整个实验中，我们专注于将Shapley方法的性能与留一法（LOO）方法的性能进行比较。LOO计算为感兴趣点存在和不存在时，在全数据集上训练的模型性能 \(V\) 之间的差异。

在以下所有实验中，我们有一个训练集、一个用于计算 \(V\) 的单独测试集，以及一个用于报告每个图形最终结果的保留集。我们对TMC-Shapley和G-Shapley的收敛标准是 \(\frac{1}{n}\sum_{i = 1}^{n}\frac{|\phi_{i}^{t} - \phi_{i}^{t - 100}|}{|\phi_{i}^{t}|} < 0.05\)。对于所有实验，计算数据Shapley值在四台并行运行的机器上花费了不到24小时（每台机器有4个CPU），除了其中一个实验，其中模型是Conv-Net，为此使用了4个GPU并行运行了120小时。应该提到的是，两种数据Shapley算法都可以并行化到迭代次数，因此，使用更多机器并行可以使计算更快。

### 4.1. 疾病预测的数据Shapley

在这个实验中，我们使用英国生物银行数据集（Sudlow等人，2015）；任务是使用285个特征预测个体是否会被诊断为乳腺和皮肤恶性肿瘤（ICD10代码C50和C44，二元分类）。为每个任务创建了平衡的二元数据集，我们使用1000个个体进行训练任务。逻辑回归对乳腺和皮肤癌预测分别产生了 \(68.7\%\) 和 \(56.4\%\) 的测试准确率。性能计算为训练模型在1000个单独患者上的准确率。两个任务的不同准确率使我们能够研究更准确或不太准确的分类器的数据Shapley。我们首先计算TMC-Shapley、G-Shapley和留一法值。TMC-Shapley在两个任务上都收敛于4000次蒙特卡洛迭代，而G-Shapley在1500次迭代时已经收敛。附录A显示了训练集中随机选择的数据点的收敛示例。

**有价值数据的重要性** 在计算数据价值之后，我们从训练集中从最有价值的数据开始依次移除数据点，每次都训练一个新模型。图1(a)显示了当有价值的数据点被丢弃时性能的变化；数据Shapley认为有价值的数据点对模型性能至关重要，而留一法估值仅比随机估值（即移除随机点）略好。图1(b)描绘了相反设置的结果；我们从价值最低的数据点开始移除。有趣的是，在这些训练集中具有低Shapley值的点实际上损害了模型的性能，移除它们将提高准确率。

**获取新数据** 观察哪些类型的训练数据具有高Shapley值，并告知我们如何通过招募相似的个体来收集新数据，以改进模型性能。让我们考虑以下实际场景：我们想向训练数据中添加一些新患者来改进我们的模型。添加个体有成本，所以我们必须从2000名候选者池中选择。我们进行两个实验：首先，我们尝试添加类似于高价值训练点的点，然后我们通过添加低价值点重复相同的实验。为此，我们拟合一个随机森林回归模型来计算数据Shapley值。回归模型学习在给定其可观测值的情况下预测数据点的价值。使用训练好的回归模型，我们估计患者池中患者的估计价值。图1(c)描绘了当我们向训练集中添加高估计价值患者时，模型性能如何变化；模型性能比随机添加新患者更有效地增加。考虑相反的情况，图1(d)显示，通过选择错误的患者添加，我们实际上可能会损害当前模型的性能。

### 4.2. 合成数据

我们使用合成数据进一步分析Shapley值。数据生成过程如下。首先，特征从50维高斯分布 \(\mathcal{N}(0, I)\) 中采样。然后，每个样本 \(i\) 的标签被分配一个二元标签 \(y_{i}\)，其中 \(P(y_{i} = 1) = f()\)，对于某个函数 \(f(.)\)。我们创建两组数据集：20个数据集是特征-标签关系线性的（线性 \(f(.)\)），以及20个数据集其中 \(f(.)\) 是三次多项式。对于第一组数据集，我们使用逻辑回归模型，对于第二组，我们同时使用逻辑回归和一个具有一个隐藏层的神经网络。然后我们开始从最有价值到最无价值地移除训练点，并跟踪模型性能的变化。图2显示了使用训练数据大小为100和1000的结果；在所有设置中，Shapley估值方法在确定对模型性能有最积极影响的数据点方面比留一法做得更好。注意这里Shapley值总是依赖于所选模型：在一个具有
<center>**图1. 疾病预测** 对于乳腺和皮肤癌预测任务，我们使用TMC-Shapley、G-Shapley和留一法（LOO）计算训练集中每个点的价值。(a) 我们从训练集中移除最有价值的数据，根据三种方法加上均匀抽样的排名。Shapley方法识别了重要的数据点，移除最有价值的TMC-Shapley或G-Shapley点导致性能比随机移除数据更差。这对LOO不成立。(b) 从训练集中移除低TMC-Shapley或G-Shapley价值的数据提高了预测器的性能。(c) 我们获得了与训练数据中高TMC-Shapley或G-Shapley价值患者相似的新患者。与添加随机患者相比，这导致了更大的性能提升。(d) 获取与低TMC-Shapley或G-Shapley价值患者相似的新患者没有帮助。</center>

非线性特征-标签关系的数据集中，将提高非线性模型性能的数据点可能对线性模型有害，因此没有价值。

### 4.3. 标签噪声

使用众包标注数据集容易出错（Frénay & Verleysen, 2014），错误标记数据可以作为一种简单的数据投毒方法（Steinhardt等人，2017）。在这个实验中，给定一个带有噪声标签的训练数据，我们从最无价值到最有价值地检查并纠正错误标记的示例，因为我们期望错误标记的示例在最无价值的点中（有些具有负的Shapley值）。图3显示了使用TMC-Shapley、梯度Shapley（如果适用）和留一法方法与随机检查基准相比的这种方法的有效性。我们对三个不同的数据集和三个不同的预测模型进行了实验。在第一个实验中，我们使用垃圾邮件分类数据集（Metsis等人，2006）。3000个数据点用于训练一个多项式朴素贝叶斯模型，该模型将电子邮件的词袋表示作为输入。我们随机翻转了 \(20\%\) 的训练点的标签。TMC-Shapley在5000次迭代中收敛。在下一个实验中，我们使用具有5个不同类别的花卉图像分类数据集。我们将花卉图像通过Inception-V3模型，并在学习到的网络表示上训练一个多项式逻辑回归模型，使用1000张图像，其中 \(10\%\) 的图像被错误标记。两种Shapley算法都在2000次迭代中收敛。最后，我们在来自Fashion MNIST数据集（Xiao等人，2017）的1000张图像上训练一个具有一个卷积层和两个前馈层的卷积神经网络，以分类T恤和上衣与衬衫。\(10\%\) 的数据点有翻转的标签。TMC-Shapley和G-Shapley都在2000次迭代中收敛。价值是在大小为1000的单独集合上计算的。图3显示了结果。图4显示了花卉和Fashion MNIST数据集中5个TMC-Shapley值最低的图像，其中所有都是错误标记的示例。

### 4.4. 数据质量与价值

在这个实验中，我们使用了（Koh & Liang, 2017）中介绍的狗与鱼数据集。对于每个类别，从ImageNet（Russakovsky等人，2015）中提取了1200张图像。我们
<center>**图2. 合成实验** 对三种不同设置显示了平均结果。垂直轴是相对准确率，代表准确率除以未进行任何移除时在全训练数据上训练的模型的准确率。每个图使用了20个数据集。在所有数据集中，生成过程如下：对于输入特征，标签生成使得 \(p(y|x) = f(x)\)，其中在(a)中 \(f(.)\) 是线性的，在(b)中 \(f(.)\) 是三次多项式，(c)使用与(b)相同的数据集。在(a)和(b)中模型是逻辑回归，在(c)中是神经网络。两种Shapley方法在将高价值分配给对模型性能有最高积极影响的数据点方面做得更好。彩色阴影区域代表20个数据集结果的标准差。</center>

使用了一个最先进的Inception-v3网络（Szegedy等人，2016），除了顶层外所有层都被冻结。随机采样100张图像作为训练集，1000张图像用于计算价值函数。我们通过添加白噪声破坏了 \(10\%\) 的训练数据，并计算干净和噪声图像的平均TMC-Shapley值，并用不同的噪声水平重复相同的实验。如图5(a)所示，随着噪声水平增加（数据质量下降），噪声图像的数据Shapley值下降。

### 4.5. 组Shapley值

在这个实验中，我们使用医院再入院数据集（Strack等人，2014）的平衡子集进行患者再入院的二元预测。我们根据性别、种族和年龄的人口统计特征将患者分组为146组。在大小为60000的训练集上训练的梯度提升分类器产生了 \(58.4\%\) 的准确率。然后我们计算组的TMC-Shapley值。图5(b)显示，最有价值的组也是模型性能最重要的组。除了计算效率之外，组Shapley的一个重要优势是其易于解释。例如，在这个数据集中，老年患者组比年轻患者组具有更高的价值，少数族裔获得的价值较低，女性组在数据Shapley方面往往比男性组更有价值，等等。

## 5. 相关工作

Shapley值是在博弈论的一篇经典论文中提出的（Shapley, 1953），并在经济学中具有广泛影响力（Shapley等人，1988）。它已被应用于分析和建模各种问题，包括投票、资源分配和议价（Milnor & Shapley, 1978; Gul, 1989）。据我们所知，Shapley值尚未像我们这样用于在机器学习上下文中量化数据价值。Shapley值最近被提出作为特征重要性分数，用于解释黑盒预测模型（Kononenko等人，2010; Datta等人，2016; Lundberg & Lee, 2017; Cohen等人，2007; Chen等人，2018; Lundberg等人，2018）。他们的目标是为给定的预测量化哪些特征对模型输出最有影响。我们的目标非常不同，因为我们旨在量化单个数据点（而非特征）的价值。还有文献使用蒙特卡洛方法、网络近似以及在专门设置中解析求解Shapley值来估计Shapley值（Fatima等人，2008; Michalak等人，2013; Castro等人，2009a; Maleki等人，2013; Hamers等人，2016）。

在线性回归中，Cook距离衡量删除一个点对回归模型的影响（Cook, 1977）。杠杆和影响是相关的概念，衡量扰动每个点如何影响模型参数和模型对其他数据的预测（Cook & Weisberg, 1982; Koh & Liang, 2017）。然而，这些方法不
<center>**图3. 纠正翻转的标签** 我们从最无价值到最有价值地检查训练数据点，并修正错误标记的示例。如图所示，Shapley值方法最早检测到错误标记的示例。虽然留一法在逻辑回归模型上表现相当好，但它在另外两个模型上的性能类似于随机检查。</center>

<center>**图4. 标签噪声与Shapley值** 具有最低TMC-Shapley值的图像。所有都是错误标记的。</center>

满足任何公平性条件，并且已被证明具有鲁棒性问题（Ghorbani等人，2017）。在广泛的讨论中，数据的价值以及个人应如何获得补偿已被经济学家和政策制定者激烈讨论，同时讨论了激励参与者生成有用数据（Arrieta Ibarra等人，2017; Posner & Weyl, 2018）。

## 6. 讨论

我们提出了数据Shapley作为量化单个训练数据对学习算法价值的公平框架。数据Shapley独特地满足了公平数据估值的三个自然特性。在一些ML设置中，这些特性可能不是期望的，也许需要添加其他特性。未来的一个重要方向是清楚地理解这些不同的场景，并研究适当的数据价值概念。借鉴经济学的联系，我们相信我们列出的三个特性是一个合理的起点。虽然我们的实验证明了数据Shapley的几个期望特性，但我们应该谨慎地解释它。由于篇幅限制，我们忽略了许多关于个人数据内在价值的重要考虑，我们专注于监督学习算法训练集这个非常具体的背景下的估值。我们承认数据的价值存在细微差别——例如隐私、个人关联——没有被我们的框架捕捉。此外，我们不建议人们应该完全按其数据Shapley值获得补偿；我们相信数据Shapley更有用的是它提供的定量见解。

<center>**图5. (a) 价值与数据质量**：向 \(10\%\) 的训练点添加白噪声。随着噪声水平增加，噪声图像的平均TMC-Shapley值与干净图像相比下降。(b) 组Shapley：移除有价值的组比移除具有最高留一法分数的组更能降低性能。</center>

在数据Shapley框架中，单个数据的价值取决于学习算法、评估度量以及训练集中的其他数据点。因此，当我们讨论具有高（或低）Shapley值的数据时，所有这些上下文都被假定为给定。在一个上下文中没有价值的数据在上下文改变时可能非常有价值。理解数据Shapley对于不同学习函数和度量的行为是一个有趣的后续工作方向。
1

## 参考文献

<参考文献列表，保留原格式>